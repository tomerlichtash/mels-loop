import { default as PopoverTrigger } from './PopoverTrigger';
import { default as PopoverDialog } from './PopoverDialog';
import { default as PopoverCloseButton } from './PopoverCloseButton';

export default {
	PopoverTrigger,
	PopoverDialog,
	PopoverCloseButton,
};

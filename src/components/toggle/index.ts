import * as ToggleGroupPrimitives from "@radix-ui/react-toggle-group";
import { createCompRef } from "../create-ref";

export const ToggleGroup = createCompRef(ToggleGroupPrimitives.Root);
export const ToggleGroupItem = createCompRef(ToggleGroupPrimitives.Item);

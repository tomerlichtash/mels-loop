version: 2.1
jobs:
  lint:
    docker:
      - image: cimg/node:18.17
    steps:
      - checkout
      # - restore_cache:
      #     name: Restore npm Package Cache
      #     keys:
      #       - npm-packages-cache-lint-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.npm/cache
      - run:
          name: Install Dependencies
          command: npm install #--frozen-lockfile --cache-folder ~/.cache/npm
      # - save_cache:
      #     name: Save npm Package Cache
      #     key: npm-packages-cache-lint-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.cache/npm
      # - restore_cache:
      #     name: scripts - Restore npm Package Cache
      #     keys:
      #       - npm-packages-cache-lint-scripts-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.npm/cache
      # - run:
      #     name: scripts - Install Dependencies
      #     command: cd scripts && npm install --frozen-lockfile --cache-folder ~/.cache/npm-scripts
      # - save_cache:
      #     name: scripts - Save npm Package Cache
      #     key: npm-packages-cache-lint-scripts-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.cache/npm-scripts
      - run:
          name: Lint
          command: npm lint
  unit:
    docker:
      - image: cimg/node:18.17
    steps:
      - checkout
      # - restore_cache:
      #     name: Restore npm Package Cache
      #     keys:
      #       - npm-packages-unit-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.npm/cache
      - run:
          name: Install Dependencies
          command: npm install #--frozen-lockfile --cache-folder ~/.cache/npm
      # - save_cache:
      #     name: Save npm Package Cache
      #     key: npm-packages-unit-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.cache/npm
      - run:
          name: Unit Test
          command: npm test:unit
      - store_artifacts:
          path: coverage

      # - store_test_results:
      #     path: unit-tests-results.xml
  e2e:
    docker:
      - image: mcr.microsoft.com/playwright:v1.41.2-jammy
    parallelism: 4
    steps:
      - checkout
      # - restore_cache:
      #     name: Restore npm Package Cache
      #     keys:
      #       - npm-packages-playwright-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.npm/cache
      - run:
          name: Install Dependencies
          command: npm install #--frozen-lockfile --cache-folder ~/.cache/npm
      # - save_cache:
      #     name: Save npm Package Cache
      #     key: npm-packages-playwright-{{ .Branch }}-{{ checksum "npm.lock" }}
      #     paths:
      #       - ~/.cache/npm
      - run:
          name: E2E Test
          command: SHARD="$((${CIRCLE_NODE_INDEX}+1))"; npm test:e2e --shard=${SHARD}/${CIRCLE_NODE_TOTAL}
      # - store_artifacts:
      #     path: playwright-report
      #     destination: playwright-report

workflows:
  CI:
    jobs:
      - lint
      - unit
      - e2e
